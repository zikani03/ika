{
  "name": "@nndi/ika",
  "version": "0.1.0",
  "description": "Form filling just got faster, run regular users to power users!",
  "main": "ika.js",
  "scripts": {
    "test": "jest",
    "build": "babel ika.js ika.dom.js -o build/ika.min.js && uglifyjs build/ika.min.js -o build/ika.min.js && cleancss ika.css -o build/ika.min.css",
    "demo": "cp build/* demo/"
  },
  "keywords": [
    "ika",
    "form",
    "tags",
    "input"
  ],
  "author": "NNDI",
  "license": "MIT",
  "devDependencies": {
    "@babel/cli": "^7.19.3",
    "@babel/core": "^7.9.0",
    "@babel/preset-env": "^7.9.0",
    "babel-preset-minify": "^0.5.1",
    "clean-css-cli": "^4.3.0",
    "jest": "^29.3.1",
    "uglify-js": "^3.8.1"
  }
}
